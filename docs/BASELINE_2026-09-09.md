# 2026-09-09 重构前基线

## 保留内容

- `mvp_phase1/backend.py`：HDC 命令、dumpLayout 和 bounds 解析原型。
- `tests/smoke/recognize_zhihu_plus.py`：知乎++固定流程探针。
- `target_app_profile.json`：包含动态观测的原始档案，仅作为历史证据。
- `artifacts/phase1`：本机保留的原始命令、布局、元素和探测日志，默认不提交 Git。
- `tests/fixtures/legacy/zhihu-plus`：从最后一次完整 phase1 流程中精选并校验 SHA-256 的可复用样本。

## 已确认事实

- HDC 通过 `HDC_PATH` 配置；本机验收使用 DevEco Studio OpenHarmony SDK 中的 `hdc.exe`。
- 当前设备：`127.0.0.1:5555`。
- 当前设备类型：phone。
- 当前渲染分辨率：1320 × 2232。
- 已有探针能够执行启动、布局采集、点击、输入和返回。
- 历史 artifacts 只有 JSON 与日志；旧截图未通过 file recv 拉回本机。

## 未完成基线

- Hypium 6.1.0.210 导入会使用用户目录下的 `.xdevice`；新 Runner 必须注入项目内 `HOME` 和 `USERPROFILE`。
- 原 probe 日志停留在 UITest Agent 文件发送阶段，没有 Runner 成功报告。
- 原工程没有 Pydantic AI、FastAPI、Web、页面图或代码生成实现。
- 仓库在重构开始时没有任何 Git 提交；本轮按用户授权建立分批提交，但不执行 `git push`。

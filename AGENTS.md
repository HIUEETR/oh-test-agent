# AGENTS.md

## 开发与 Git 工作流

- 开发新功能、修复缺陷或进行会改变项目行为的重构时，必须先从最新的 `main` 创建独立分支，不得直接在 `main` 上开发。
- 分支名使用 `topic` 格式，例如 `api-timeout-fix`。开始工作前先确认当前分支和工作区状态；如果当前不是目标工作分支，先切换或创建合适的分支。
- 只提交与当前任务相关的文件。不要提交 `.env`、运行产物、截图、日志、缓存、密钥或其他被 `.gitignore` 排除的本地文件。
- 除非用户明确要求，否则不要执行 `git push`。合并前必须保留清晰的变更范围和验证记录。

## 完成门禁

在宣称任务完成或准备合并前，必须根据改动范围执行并确认通过以下检查：

```powershell
uv lock --check
.\.venv\Scripts\ruff.exe check .
.\.venv\Scripts\ruff.exe format --check .
.\.venv\Scripts\python.exe -m pytest -q
Push-Location web
npm run build
npm run test
Pop-Location
git diff --check
```

- 如果改动只涉及文档，也至少执行 `git diff --check`，并检查 Markdown 内容和链接；如果改动涉及 Python、前端、配置或依赖，必须执行对应的完整检查，并在必要时补充针对性测试。
- 真实设备、Hypium、真实 Provider 或浏览器验收属于相关功能的额外门禁。改动影响这些链路时，不能只用静态检查代替相应验收；测试因环境条件无法执行时，必须明确记录为未验证，不得描述为通过。
- 任何测试、Ruff、格式检查、构建、锁文件检查或 diff 检查失败，都必须先修复或明确记录阻塞原因，不得进入合并步骤。
- 合并前确认工作区没有未预期的修改，检查待合并 diff，并确认测试结果对应当前分支最新提交。

## 合并门禁

- 只有在完成门禁全部通过、变更范围已复核、没有未解决冲突且分支包含最新 `main` 后，才允许合并到 `main`。
- 优先使用非快进合并保留分支历史；合并前先同步并检查 `main`，再将最新 `main` 合并或变基到功能分支，解决冲突后重新执行受影响的全部检查。
- 不得使用 `git merge --no-verify`、跳过测试、跳过 Ruff、跳过构建或强制覆盖冲突来绕过门禁。
- 合并完成后必须再次检查 `git status`、`git log` 和必要的验证结果，确认合并结果仍然通过；只有确认成功后才能向用户报告已合并。
- 未经用户明确要求，不要删除功能分支、修改远程仓库、推送远程或创建发布版本。

## 完成汇报

完成任务时应说明：使用的分支、实际修改的文件、执行过的检查及结果、未执行或未通过的验收项，以及是否已经合并到 `main`。不能把“代码已修改”或“测试未执行”表述为任务已完成。
